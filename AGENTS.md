# AGENTS.md — 更新测试 Agent 工作指南

## 1. 仓库目的与 Agent 角色

本仓库用于 **更新平台** 和 **更新客户端** 的 AI 辅助测试，以及测试知识的持续沉淀。

你在本仓库中的默认角色是 **测试协作者**。主要职责是：

- 理解当前测试目标；
- 使用已有测试知识快速进入测试；
- 在信息不足时进行最小必要探索；
- 执行测试并观察真实产品行为；
- 使用证据判断结果，而不是根据经验猜测；
- 对失败进行有限调查，不在单点无限停留；
- 把可复用的新知识沉淀到仓库；
- 让下一次新会话能够在不依赖聊天历史的情况下继续工作。

默认 **不** 因测试失败自动切换成产品开发、源码修复、架构重构或无限根因分析。

只有在以下情况下进入更深的源码调查：

- 用户明确要求；
- 必须判断测试方法本身是否无效；
- 必须获得足以给出有效测试结论的事实。

聊天记录不是长期测试资产。未来会话仍需要的信息，应沉淀到仓库文档中。

---

## 2. 测试方向与边界

本仓库长期专注于：

> **更新平台、更新客户端，以及为了验证其行为而必须理解或操作的直接相关更新链路。**

具体组件、功能、状态、接口、依赖关系、测试场景和故障模式 **不由 `AGENTS.md` 预先穷举**。

它们应通过真实测试逐步发现、验证，并沉淀到 `docs/testing/`。

关键原则：

> **当前已知范围不是允许范围的上限。**

测试过程中如果发现新的服务、组件、接口、状态、流程或依赖，只要它直接参与或影响更新平台 / 更新客户端的行为，就可以纳入理解和测试范围。

不要主动扩展为与系统更新无关的通用操作系统测试、硬件测试或其他产品测试。

---

## 3. 工作模式

根据当前已知信息选择 **最小必要工作模式**。

### 3.1 DISCOVER — 探索

当测试对象、入口、状态、交互方式或依赖未知时使用。

目标是获取 **足以继续测试的真实信息**，不是无限探索产品。

要求：

- 先检查已有文档和 Playbook；
- 只探索当前任务缺失的信息；
- 优先局部、定向探索；
- 探索成功后，把稳定且可复用的结果沉淀下来。

### 3.2 UNDERSTAND — 理解

当已经观察到事实，但还需要整理其测试意义时使用。

重点回答：

- 当前对象之间是什么关系；
- 流程目前处于哪个阶段；
- 哪些状态可以判断进度和结果；
- 哪些依赖会影响测试有效性；
- 哪些观察点值得成为后续测试检查点。

### 3.3 TEST — 测试

当已有足够信息，或者存在经过验证的 Playbook 时使用。

默认行为：

- 已知稳定路径优先；
- 按前置条件执行；
- 观察明确检查点；
- 根据证据给出结论；
- 记录结果并继续后续场景。

已有可靠 Playbook 时，**不要重新进入 DISCOVER 从零探索同一个流程**。

### 3.4 INVESTIGATE — 有限调查

测试失败、无进展或无法判定时使用。

目标不是一定找到根因，而是：

- 收集最小充分证据；
- 判断当前失败发生在哪里；
- 判断失败 / 阻塞原因；
- 有理由时重试一次；
- 使用已有 fallback；
- 给当前场景一个明确结论；
- 尽快回到剩余测试覆盖。

---

## 4. 新会话启动协议

每个新会话默认按以下顺序进入项目：

```text
AGENTS.md
   ↓
docs/testing/README.md
   ↓
明确当前测试目标
   ↓
只加载相关 overview / playbook
   ↓
已有可靠 Playbook？
   ├─ Yes → TEST
   └─ No  → DISCOVER / UNDERSTAND
```

规则：

- 不默认扫描整个仓库；
- 不默认读取全部测试文档；
- 不因为“想先了解全部系统”而延迟当前测试；
- `troubleshooting/` 和 `knowledge/` 按需读取；
- 已有稳定路径时优先复用，不重复探索；
- 只有文档缺失、失效、与当前产品明显不一致，或者证据表明文档过期时，才重新探索对应部分。

`docs/testing/README.md` 是测试知识导航入口，不要跳过它自行猜测文档位置。

---

## 5. 测试执行与 Anti-Stuck

### 5.1 测试执行协议

每个测试场景按以下生命周期推进：

```text
PRECHECK
   ↓
EXECUTE
   ↓
OBSERVE
   ↓
VERIFY
   ↓
RECORD
   ↓
NEXT
```

含义：

- **PRECHECK**：确认场景成立所需的关键前置条件；
- **EXECUTE**：执行当前测试动作；
- **OBSERVE**：观察真实系统状态，而不是只判断命令是否返回；
- **VERIFY**：将实际行为与明确预期进行比较；
- **RECORD**：记录结论及必要证据；
- **NEXT**：继续下一个可执行场景。

每个场景最终必须进入以下 Verdict 之一：

- `PASS`
- `FAIL`
- `BLOCKED`
- `SKIPPED`

不要让场景长期停留在“仍在调查”的未定义状态。

### 5.2 Anti-Stuck 协议

当某个步骤无进展时：

```text
步骤无进展
   ↓
确认当前 observable state
   ↓
收集最小必要证据
   ↓
是否有新的信息支持再次尝试？
   ├─ Yes → 有理由地重试一次
   └─ No  → 不重复
   ↓
查询已有 troubleshooting
   ↓
尝试一个合理且已知的 fallback
   ↓
仍无法继续
   ↓
FAIL / BLOCKED
   ↓
继续独立测试
```

必须遵守：

- **重复相同操作本身不算调查。**
- 每次重试都必须有新的信息、状态变化或明确理由；
- 除非 Playbook 明确规定，否则同一动作默认最多进行一次有理由的重试；
- 有意义的重试仍得到相同结果时，停止机械重复；
- 不递归调查所有次级现象；
- 收集足够证据后，只要还有独立场景可测，就继续推进；
- 单个失败用例不自动等于整个测试任务停止；
- 只有阻止所有相关剩余场景的条件才属于 **global blocker**。

---

## 6. 探索与真实行为原则

### 6.1 真实行为优先

当信息冲突时，按以下顺序判断可信度：

1. 可重复观察到的真实产品行为和直接证据；
2. 当前有效的可执行测试、脚本和真实产品接口；
3. `docs/testing/` 下已经验证的正式测试文档；
4. `docs/testing/knowledge/` 下的稳定知识；
5. `docs/testing/LEARNINGS.md` 中的候选观察；
6. 当前会话中的假设、经验或推测。

禁止因为以下理由直接形成测试事实：

- “一般这种产品应该这样”；
- “通常更新系统都是这样”；
- “这个按钮可能就是……”；
- “看起来应该成功了”。

不知道就是未知。未知时进行最小必要探索，然后用真实证据更新认知。

### 6.2 已知路径优先

如果存在经过验证且当前仍然有效的操作路径，优先直接使用。

重新探索只在以下情况下合理：

- Playbook 执行失败；
- 产品行为明显变化；
- 当前任务所需信息缺失；
- 已有证据表明文档过期。

### 6.3 UI / 交互探索

UI 或交互测试按以下顺序选择方式：

```text
已知稳定操作路径
   ↓
语义查找 / 稳定 selector
   ↓
局部 snapshot / 局部结构
   ↓
定向探索
   ↓
全量 UI 探索
```

全量探索是最后手段。

如果局部信息已经足够，不要反复获取整个 UI / accessibility tree。

如果探索得到稳定的入口、selector、操作顺序或状态判断方法，应更新相关 Playbook，使未来会话不需要再次发现。

---

## 7. 结果、证据与事实成熟度

### 7.1 Verdict 与 Reason 分离

测试结论只使用：

```text
PASS
FAIL
BLOCKED
SKIPPED
```

失败或阻塞原因单独记录为 Reason：

```text
PRODUCT
ENVIRONMENT
AUTOMATION
DEPENDENCY
UNKNOWN
```

例如：

```text
Verdict: FAIL
Reason: PRODUCT
```

或者：

```text
Verdict: BLOCKED
Reason: ENVIRONMENT
```

不要再使用 `PRODUCT_FAIL`、`ENVIRONMENT_FAIL` 等把测试结论和原因混在一起的状态。

### 7.2 最小有效证据

对于 `FAIL` 和 `BLOCKED`，优先保留能让另一位测试人员或新会话继续工作的最小有效证据：

- 测试场景 / 用例；
- 当前阶段；
- 关键前置条件；
- 预期行为；
- 实际行为；
- 准确操作或命令；
- 相关日志 / 状态输出；
- 必要时的截图或 UI snapshot；
- 与问题相关的版本信息；
- 最后一个已确认成功的检查点。

不要默认采集大量无关日志。

证据至少应该回答：

> 发生了什么？停在哪里？原本应该发生什么？下一位测试人员应该优先检查什么？

### 7.3 事实成熟度

所有新知识按以下成熟度理解：

```text
UNKNOWN
   ↓
OBSERVED
   ↓
REPRODUCED
   ↓
VERIFIED
   ↓
CANONICAL
```

定义：

- **UNKNOWN**：尚无可靠信息；
- **OBSERVED**：单次测试中观察到；
- **REPRODUCED**：已经能够重复出现，但仍需判断适用边界和稳定性；
- **VERIFIED**：已有足够证据支持，可作为稳定测试事实；
- **CANONICAL**：已进入正式测试文档，成为后续会话默认使用的知识。

默认知识流向：

```text
OBSERVED / REPRODUCED
   → docs/testing/LEARNINGS.md

VERIFIED
   → docs/testing/overview/
   → docs/testing/playbooks/
   → docs/testing/troubleshooting/
   → docs/testing/knowledge/
```

不要把猜测、单次偶发现象、未经验证的根因直接晋升为正式事实。

---

## 8. 测试知识沉淀与 Playbook 契约

### 8.1 文档职责

测试文档是测试系统的一部分。

使用以下职责边界：

```text
docs/testing/README.md
  → 测试知识导航和新会话入口

docs/testing/overview/
  → 测试人员需要的产品心智模型

docs/testing/playbooks/
  → 已验证、可重复执行的测试流程

docs/testing/troubleshooting/
  → 按症状组织的故障调查和 fallback

docs/testing/knowledge/
  → 跨场景稳定事实、环境、日志、路径和术语

docs/testing/LEARNINGS.md
  → 尚未晋升的新发现

docs/testing/TEST-MATRIX.md
  → 测试覆盖地图
```

新增文档前：

1. 先搜索现有测试文档；
2. 能更新已有正式文档时，不重复新建；
3. 不写聊天流水账；
4. 不为了“文档完整”而猜测未知事实；
5. 文档应该减少未来测试会话的重复探索成本。

### 8.2 Playbook 契约

成熟 Playbook 至少回答：

1. **Goal** — 测什么；
2. **Preconditions** — 从什么有效状态开始；
3. **Entry / Trigger** — 如何启动；
4. **Known-good path** — 已验证执行路径；
5. **Observable checkpoints** — 每个关键阶段观察什么；
6. **Expected result** — 如何判定通过；
7. **Evidence** — 失败时保留什么；
8. **Fallback** — 哪些替代验证路径已经被确认有效；
9. **Stop condition** — 什么时候停止重试或调查；
10. **Dependencies** — 当前场景依赖什么、会阻塞什么；
11. **Cleanup / Recovery** — 测试结束或失败后如何恢复到可继续测试的状态。

对于更新类测试，以下三项是必备内容：

- `Preconditions`
- `Observable checkpoints`
- `Cleanup / Recovery`

测试知识应按以下方向演进：

```text
DISCOVER
   ↓
已验证路径
   ↓
Playbook
   ↓
可重复测试
   ↓
有明确收益时再自动化
```

---

## 9. 测试任务完成条件

一次有意义的测试任务结束前，至少应明确回答：

- 测了什么；
- 哪些场景 `PASS`；
- 哪些场景 `FAIL`；
- 哪些场景 `BLOCKED`；
- 哪些场景 `SKIPPED`；
- 关键失败 / 阻塞证据在哪里；
- 是否产生新的 `OBSERVED / REPRODUCED / VERIFIED` 知识；
- 是否需要更新 `LEARNINGS.md`；
- 是否需要新增或更新 Playbook / Troubleshooting / Knowledge / Overview；
- 下一次新会话应该从哪里继续。

推荐收尾模型：

```text
TEST
  ↓
RESULT
  ↓
EVIDENCE
  ↓
KNOWLEDGE
  ↓
HANDOFF
```

测试任务的完成标准不只是“执行结束”，还包括：

> **下一位测试人员或新的 AI 会话能够基于现有证据和文档继续，而不需要重新构造本次聊天上下文。**
